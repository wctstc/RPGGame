#pragma once

#define MACRO_2_STRING(macro) MACROSTRING(macro)
#define MACROSTRING(macro) #macro


#define CLASS1( type, prefix, format )


#define VARIABLEN(n)


#define VARIABLE(type, prefix, format)


class DataConfig
{
public:

    DataConfig()
    {
    }

    ~DataConfig()
    {
    }
};

